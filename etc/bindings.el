;; bindings.el
;;; put all global key bindings here (so as to avoid binding conflicts)

;;; GLOBAL ONLY -- mode-specific in mode-specific files
(global-set-key (kbd "RET")     'reindent-then-newline-and-indent)
